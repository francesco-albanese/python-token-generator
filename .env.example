# This must be changed with your connection ID value
CONNECTION_ID=

# Optional: Set log level (default: INFO)
# LOG_LEVEL=INFO

# Optional: JWT audience claim (default: https://api.example.com)
# AUDIENCE=https://api.example.com

# Optional: Token expiry time in minutes (default: 5)
# TOKEN_EXPIRY_MINUTES=5

# Optional: RSA key size in bits (default: 4096)
# KEY_SIZE=4096

# Optional: Certificates directory (default: ./certificates)
# CERTIFICATES_DIR=./certificates
